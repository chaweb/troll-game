console.log("socket lancé !")

module.exports = {
    logger: logger,
    default : function Svc(socket, io) {
        return Object.freeze({
            async game(data){ 
                
            },
            
        })
    }
};

